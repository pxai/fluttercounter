version: '2.1'
jobs:
  build:
    docker:
      - image: mobiledevops/flutter-sdk-image:3.16.4
    steps:
        - checkout
        - run:
            name: Flutter Build
            command: flutter build apk
# workflows:
#   test:
#     jobs:
#       - flutter/unit_test
